<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/512/1692/1692816.png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2? family= Bitter:ital,wght@1,700 & family= Montserrat:wght@300;400;500;600 & display=swap');
        body{
            background-color:  rgb(18, 47, 124);
            color: white;
            font-family: 'Montserrat', sans-serif;
            font-weight: 300;
            font-size: 16px;
            line-height: 1.5;
            overflow-x: hidden;
            margin: 2%;
        }
        body::after, body::before{
            content: '';
            position: fixed;
            height: 900px;
            width: 900px;
            border-radius: 50%;
            z-index: -1;
            filter: blur(150px);
            opacity: 0.5;
        }
        body::before{
            background-color: rgb(28, 28, 171);
            left: 0;
            bottom: 0;
            transform: translate(-50%);
        }
        body::after{
            background-color: rgb(62, 3, 118);
            right: 0;
            top: 0;
            transform: translate(50%);
        }
        .form-main{
            min-height: 100vh;
            padding: 60px 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        .form-conte{
            width: 100%;
            max-width: 450px;
            position: relative;
            z-index: 1;
        }
        .form-conte .circle-1,
        .form-conte .circle-2,
        .form-conte .circle-3{
            position: absolute;
            background: linear-gradient(to right, rgb(55, 167, 242), rgb(23, 213, 143));
            border-radius: 50%;
            z-index: -1;
        }
        .form-conte .circle-1{
            height: 150px;
            width: 150px;
            left: 0;
            top: 10%;
            transform: translateX(-50%);
        }
        .form-conte .circle-2{
            height: 100px;
            width: 100px;
            right: 0;
            bottom: 0;
            transform: translate(30%);
        }
        .form-conte .circle-3{
            height: 50px;
            width: 50px;
            right: 10%;
            top: 10%;
        }
        .form-conte .box{
            border: 1px solid rgb(147, 142, 142);
            border-radius: 40px;
            padding: 40px 50px;
            backdrop-filter: blur(10px);
        }
        .form-conte h3{
            font-size: 28px;
            text-align: center;
            line-height: 1.2;
            color: white;
            font-weight: 500;
        }
        .form-conte form{
            margin: 25px 0 20px;
        }
        .form-conte .input-box{
            margin-bottom: 25px;

        }
        .form-conte .input-control{
            display: block;
            height: 45px;
            width: 100%;
            background-color: transparent;
            border: none;
            border-bottom: 1px solid rgb(147, 142, 142);
            color: inherit;
            font-weight: inherit;
            font-family: inherit;
            font-size: 16px;
        }
        .form-conte .input-control:focus{
            outline: none;
        }
        .form-conte .btn{
            height: 45px;
            width: 100%;
            font-size: 16px;
            font-weight: 500;
            text-transform: capitalize;
            font-family: inherit;
            cursor: pointer;
            border-radius: 25px;
            user-select: none;
            background: linear-gradient(to right, rgb(86, 45, 247), rgb(121, 96, 220));
            border: none;
            color: white;
            position: relative;
            z-index: 1;
            overflow: hidden;
        }
        .form-conte .btn::before{
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 100%;
            transition: background-color 0.5s ease;
            z-index: -1;
        }
        .form-conte .btn:hover::before{
            background-color: hsla(0, 0%, 0%, 0.3) ;
        }
        .bi-lock-fill{
            position: absolute;
            margin-top: -35px;
            transform: translate(330px);
        }
        .bi-person-fill{
            position: absolute;
            margin-top: -35px;
            transform: translate(330px);
        }
        .btn-1{
            margin: 20%;
            text-decoration: none;
            padding: 100%;
            font-size: 1.2rem;
            position: relative;
            margin: 32px;
            background-color: rgb(77, 106, 220);
            color: white;
            font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
            border-radius: 50px;
            transition: transform 0.3s ease;
            transform: translateY(-30%);
        }

    </style>
</head>
<body>
    <a href="/" class="btn-1 " style=" padding: 20px 30px;font-size: 1.4rem; transform: translate(-700%); ">Inicio</a>
    <section class="form-main">
        <div class="form-conte">
            <div class="circle-1"></div>
            <div class="circle-2"></div>
            <div class="circle-3"></div>
            <div class="box">
                <h3>Bienvenido</h3>
                <form action="/login" method="post">
                    <div class="input-box">
                        <input type="text" placeholder="Usuario" id="usuario" name="usuario" class="input-control">
                        <i class="bi bi-person-fill"></i>
                    </div>

                    <div class="input-box">
                        <input type="password" placeholder="Contraseña" id="contraseña" name="contraseña" class="input-control">
                        <i class="bi bi-lock-fill"></i>

                    </div>
                    <button type="submit" class="btn">Iniciar Sesión</button>
                </form>
            </div>
        </div>

    </section>
    
</body>
</html>